{
    "name": "pr-preview",
    "module": "main.ts",
    "type": "module",
    "scripts": {
        "build": "bun build --compile --minify --sourcemap --bytecode ./main.ts --outfile ./dist/main"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "@actions/core": "^1.11.1",
        "@actions/github": "^6.0.0"
    }
}
