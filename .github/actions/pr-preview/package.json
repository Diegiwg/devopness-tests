{
    "name": "pr-preview",
    "module": "main.ts",
    "type": "module",
    "scripts": {
        "build": "bun install && bun build --minify --bytecode --target node --entrypoints ./main.ts --outdir ./dist"
    },
    "devDependencies": {
        "@types/bun": "latest"
    },
    "peerDependencies": {
        "typescript": "^5.0.0"
    },
    "dependencies": {
        "@actions/core": "^1.11.1",
        "@actions/github": "^6.0.0",
        "@devopness/sdk-js": "^2.156.0"
    }
}
